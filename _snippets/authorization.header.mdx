<ParamField header="Authorization" required placeholder="Bearer YOUR_API_TOKEN">
  This is your API token; send as `Bearer YOUR_API_TOKEN`. If you don't have
  one, [get here](/api-reference/token/get).
</ParamField>
